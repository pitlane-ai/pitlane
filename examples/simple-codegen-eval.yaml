assistants:
  opencode-baseline:
    type: opencode
    args:
      model: opencode/minimax-m2.5-free

  # Uncomment to compare against other assistants (requires each tool installed):
  # claude-baseline:
  #   type: claude-code
  #   args:
  #     model: haiku

  # vibe-devstral-small:
  #   type: mistral-vibe
  #   args:
  #     model: devstral-small

  # bob-baseline:
  #   type: bob
  #   args:
  #     chat_mode: code

tasks:
  - name: hello-world-python
    prompt: "Create a Python script called hello.py that prints 'Hello, World!'"
    workdir: ./fixtures/empty
    timeout: 120
    assertions:
      - file_exists: "hello.py"
      - command_succeeds: "python3 hello.py"
