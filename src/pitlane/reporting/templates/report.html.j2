<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pitlane Report</title>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      font-size: 14px;
      line-height: 1.5;
      color: #1f2937;
      background: #f3f4f6;
    }

    /* Header */
    .header {
      background: #1a1a2e;
      color: #e5e7eb;
      padding: 16px 24px;
      display: flex;
      align-items: center;
      gap: 16px;
      flex-wrap: wrap;
    }
    .header h1 {
      font-size: 18px;
      font-weight: 600;
      color: #fff;
      flex: 1;
    }
    .header .run-dir {
      font-size: 12px;
      color: #9ca3af;
      font-family: monospace;
      word-break: break-all;
    }
    .header-badges {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
    }

    /* Summary cards */
    .summary {
      display: flex;
      gap: 16px;
      padding: 24px 24px 8px;
      flex-wrap: wrap;
    }
    .card {
      background: #fff;
      border-radius: 8px;
      box-shadow: 0 1px 3px rgba(0,0,0,.1);
      padding: 16px 20px;
      min-width: 110px;
      text-align: center;
    }
    .card .card-label {
      font-size: 11px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: .05em;
      color: #6b7280;
    }
    .card .card-value {
      font-size: 28px;
      font-weight: 700;
      margin-top: 4px;
    }
    .card.total  .card-value { color: #374151; }
    .card.passed .card-value { color: #22c55e; }
    .card.failed .card-value { color: #ef4444; }
    .card.errored .card-value { color: #f59e0b; }

    /* Suites */
    .suites {
      padding: 8px 24px 32px;
      display: flex;
      flex-direction: column;
      gap: 16px;
    }

    .suite-card {
      background: #fff;
      border-radius: 8px;
      box-shadow: 0 1px 3px rgba(0,0,0,.1);
      overflow: hidden;
    }

    .suite-summary {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 14px 18px;
      cursor: pointer;
      list-style: none;
      user-select: none;
    }
    .suite-summary::-webkit-details-marker { display: none; }
    .suite-summary::marker { display: none; }

    .suite-name {
      font-weight: 600;
      font-size: 14px;
      flex: 1;
      word-break: break-word;
    }

    .suite-time {
      font-size: 12px;
      color: #6b7280;
      white-space: nowrap;
    }

    .suite-body {
      padding: 0 18px 18px;
      border-top: 1px solid #f3f4f6;
    }

    /* Properties table */
    .props-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
      gap: 8px;
      margin: 14px 0;
    }
    .prop-item {
      background: #f9fafb;
      border-radius: 6px;
      padding: 8px 12px;
    }
    .prop-key {
      font-size: 11px;
      color: #6b7280;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: .04em;
    }
    .prop-val {
      font-family: monospace;
      font-size: 13px;
      color: #111827;
      margin-top: 2px;
    }

    /* Cases */
    .cases-header {
      font-size: 12px;
      font-weight: 600;
      color: #6b7280;
      text-transform: uppercase;
      letter-spacing: .05em;
      margin: 12px 0 8px;
    }

    .case-item {
      border-radius: 6px;
      margin-bottom: 4px;
      overflow: hidden;
    }

    .case-row {
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 7px 10px;
      background: #f9fafb;
    }
    details.case-item .case-row {
      cursor: pointer;
    }

    .case-name {
      flex: 1;
      font-size: 13px;
      word-break: break-word;
    }

    .case-message {
      background: #fef2f2;
      border-left: 3px solid #ef4444;
      padding: 8px 12px;
      font-family: monospace;
      font-size: 12px;
      color: #7f1d1d;
      white-space: pre-wrap;
      word-break: break-word;
    }

    /* Badges */
    .badge {
      display: inline-block;
      padding: 2px 8px;
      border-radius: 9999px;
      font-size: 11px;
      font-weight: 700;
      white-space: nowrap;
    }
    .badge-pass   { background: #dcfce7; color: #166534; }
    .badge-fail   { background: #fee2e2; color: #991b1b; }
    .badge-error  { background: #fef3c7; color: #92400e; }
    .badge-skip   { background: #f3f4f6; color: #6b7280; }
    .badge-neutral { background: #e5e7eb; color: #374151; }
  </style>
</head>
<body>

<header class="header">
  <div style="flex:1">
    <h1>Pitlane Report</h1>
    <div class="run-dir">{{ run_dir }}</div>
  </div>
  <div class="header-badges">
    <span class="badge badge-neutral">{{ total_tests }} tests</span>
    {% if total_failures > 0 %}
    <span class="badge badge-fail">{{ total_failures }} failed</span>
    {% endif %}
    {% if total_errors > 0 %}
    <span class="badge badge-error">{{ total_errors }} errors</span>
    {% endif %}
    {% if total_failures == 0 and total_errors == 0 %}
    <span class="badge badge-pass">all passed</span>
    {% endif %}
  </div>
</header>

<section class="summary">
  {% set total_passed = total_tests - total_failures - total_errors %}
  <div class="card total">
    <div class="card-label">Total</div>
    <div class="card-value">{{ total_tests }}</div>
  </div>
  <div class="card passed">
    <div class="card-label">Passed</div>
    <div class="card-value">{{ total_passed }}</div>
  </div>
  <div class="card failed">
    <div class="card-label">Failed</div>
    <div class="card-value">{{ total_failures }}</div>
  </div>
  <div class="card errored">
    <div class="card-label">Errors</div>
    <div class="card-value">{{ total_errors }}</div>
  </div>
</section>

<section class="suites">
  {% for suite in suites %}
  {% set suite_passed = suite.tests - suite.failures - suite.errors %}
  {% set is_failing = suite.failures > 0 or suite.errors > 0 %}
  <div class="suite-card">
    <details {% if is_failing %}open{% endif %}>
      <summary class="suite-summary">
        <span class="suite-name">{{ suite.name }}</span>
        {% if is_failing %}
        <span class="badge badge-fail">{{ suite.failures }} failed</span>
        {% else %}
        <span class="badge badge-pass">{{ suite_passed }}/{{ suite.tests }} passed</span>
        {% endif %}
        {% if suite.time %}
        <span class="suite-time">{{ "%.2f"|format(suite.time) }}s</span>
        {% endif %}
      </summary>

      <div class="suite-body">

        {% if suite.properties %}
        <div class="props-grid">
          {% for key, val in suite.properties.items() %}
          <div class="prop-item">
            <div class="prop-key">{{ key }}</div>
            <div class="prop-val">{{ val }}</div>
          </div>
          {% endfor %}
        </div>
        {% endif %}

        {% if suite.cases %}
        <div class="cases-header">Test Cases</div>
        {% for case in suite.cases %}
        {% if case.result %}
        <details class="case-item">
          <summary class="case-row">
            {% if case.result.status == "Failure" %}
            <span class="badge badge-fail">FAIL</span>
            {% elif case.result.status == "Error" %}
            <span class="badge badge-error">ERROR</span>
            {% else %}
            <span class="badge badge-skip">SKIP</span>
            {% endif %}
            <span class="case-name">{{ case.name }}</span>
          </summary>
          {% if case.result.message %}
          <div class="case-message">{{ case.result.message }}</div>
          {% endif %}
        </details>
        {% else %}
        <div class="case-item">
          <div class="case-row">
            <span class="badge badge-pass">PASS</span>
            <span class="case-name">{{ case.name }}</span>
          </div>
        </div>
        {% endif %}
        {% endfor %}
        {% endif %}

      </div>
    </details>
  </div>
  {% endfor %}
</section>

</body>
</html>
